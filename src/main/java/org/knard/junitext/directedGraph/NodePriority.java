package org.knard.junitext.directedGraph;

public enum NodePriority {

	// DON'T CHANGE THE ENUM ORDER WITHOUT REASON.
	// It has an impact on compareTo method. And it can't be overridden
	NORMAL, LOW
}
